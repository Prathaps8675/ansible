# =============================================================================
# EXAMPLE - Ansible Vault Encrypted Variables
# =============================================================================
# DO NOT use this file directly. Create the real encrypted vault file using:
#
#   ansible-vault create group_vars/rhel_servers/vault.yml
#
# Then paste these variables with your REAL values:
# =============================================================================

vault_ssh_user: "your_username"
vault_ssh_password: "your_ssh_password"
vault_sudo_password: "your_sudo_password"
